import ApiBaseService from 'resources/services/api-base-service'

export default class UserService extends ApiBaseService {
    async register(id) {
        //const response = await this.post('users/register', { externalId: id });

        //return response;
        return {};
    }
}